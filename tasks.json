{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cl", // For Visual Studio's MSVC compiler, or use "g++" for GCC
            "args": [
                "/EHsc",
                "final.cpp",
                "/Fe:final.exe" // Specify the output executable
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}